name: Deploy blog to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch: {} # dispara manualmente quando publicar algo novo no Sanity

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: Install dependencies
        working-directory: site
        run: npm ci

      - name: Build
        working-directory: site
        env:
          # Endereço público deste deploy. O site roda na raiz do domínio
          # próprio, então não há prefixo de caminho. Voltando para o endereço
          # do GitHub Pages, seria NEXT_PUBLIC_BASE_PATH: /blog e a URL de lá.
          #
          # O domínio em si não é configurado aqui nem pelo site/public/CNAME:
          # quem manda é Settings -> Pages -> Custom domain. O CNAME acompanha o
          # artefato por garantia e para deixar o endereço registrado no repo.
          NEXT_PUBLIC_BASE_PATH: ''
          NEXT_PUBLIC_SITE_URL: https://blog.valenbrasil.com
        run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
